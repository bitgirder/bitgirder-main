- reactors should reject cyclic inputs (and visit should fail when visiting one)

- casting anything to Null should work, but should discard the actual value

- ensure that Buffer.String() prints at most N leading/trailing hex chars, but
  does not attempt to hexify an enormous buffer (can always add
  Buffer.HexString() for callers that actually want that).
